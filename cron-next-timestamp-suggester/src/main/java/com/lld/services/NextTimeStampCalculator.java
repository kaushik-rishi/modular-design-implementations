package com.lld.services;

import com.lld.models.cron.Timestamp;
import com.lld.models.cron.Cron;

public class NextTimeStampCalculator {
    public String getNextTimeStamp(Timestamp t, Cron c) {
        // get the lower bound of the possible numbers in each case

        // minutes
        
    }
}
